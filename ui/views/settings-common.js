define([
	"app",
],function(app){

    var viewFragment = { template: "Common Settings ..."};

	var view = {
		$ui : viewFragment,
		$oninit:function(view){
			// nothing here
		}
	};

	return view;
});